{
  "name": "contact-api",
  "version": "1.0.0",
  "description": "Standalone contact identity service with fuzzy matching — deploy once, use from any project",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "migrate": "node migrate.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.18.2",
    "pg": "^8.11.3"
  }
}
